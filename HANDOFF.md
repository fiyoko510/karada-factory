# 引き継ぎメモ — 「からだの工場」

このプロジェクトを、別のAIまたは別の人が引き継ぐためのメモです。
これを最初に読めば、経緯・設計判断・落とし穴が一通り分かるようにしてあります。

---

## 1. これは何か

代謝と解毒を学ぶブラウザゲーム2本と、その副読本1本。
制作者はフィヨコ（デザイナー・イラストレーター、屋号トリデザ）。
「ミトコンドリア代謝をゲームで学びたい」という個人的な興味から始まりました。

公開先：https://fiyoko510.github.io/karada-factory/
リポジトリ：https://github.com/fiyoko510/karada-factory

```
index.html                  入口ページ（共有するのはこのURL）
mitochondria-kitchen.html   ミトコンドリア厨房（ATPを取り出すゲーム）
liver-workshop.html         肝臓の解毒工場（異物を処理するゲーム）
metabolism-glossary.html    用語図鑑（読み物）
ogp.png                     SNSカード画像 1200×630
assets/characters/          キャラクター画像（制作者が別途用意）
README.md / HANDOFF.md
```

**技術構成：依存ライブラリなし。1ファイル完結のHTML（HTML+CSS+JS）。ビルド不要。**
フレームワークを入れない方針です。制作者はエンジニア出身ではなく、
1ファイルをブラウザで開けば動く状態を保つことが、継続の条件になっています。

---

## 2. 譲れない方針（ここを崩さないでください）

### 2-1. 科学的に正直であること
当初の依頼は「添加物を摂ると体にどんな悪影響があるか学ぶゲーム」でしたが、
そのまま作ると事実と異なるものになるため、**軸をずらしています**。

- 認可された食品添加物のADI（一日摂取許容量）は、動物実験で無影響だった量のさらに1/100
- 日本人の実際の摂取量は、その数%程度であることが多い
- 一方で健康影響がはっきりしているのは、塩分・加工肉・糖・超加工食品というパターンのほう

そこで「添加物＝毒」ではなく、**「肝臓のラインは共有されていて有限」**を軸にしました。
ゲームを遊ぶと、添加物を全部盛りにしてもビール3杯に遠く及ばないことが数字で出ます。
これがこのゲームのいちばんの学びなので、**ここを「添加物は危険」方向に書き換えないでください。**

### 2-2. 数字は必ず検算してから渡すこと
過去に2回、致命的なバランス崩壊を出しています（詳細は §5）。
数値モデルを触ったら、**必ず全ステージ・全パターンを機械的に計算して、
クリア可能であることを確認してから**制作者に渡してください。

### 2-3. 見た目の方針
- ミトコンドリア厨房：電子顕微鏡の染色標本を思わせる深い紫。ATPだけ金色
- 肝臓の解毒工場：肝小葉モチーフのボルドー。胆汁の黄緑がアクセント
- 用語図鑑：生成り色の紙に墨。長文を読むページなので明るい配色にしている（意図的な対比）
- 見出しは明朝（Shippori Mincho B1）、本文はZen Kaku Gothic New、数値はDM Mono
- 制作者の好みは「江戸の品のあるかわいさ」と鳥モチーフ

---

## 3. ゲームの仕組み

### 3-1. ミトコンドリア厨房

献立を選ぶ → 潜在ATPを計算 → 補酵素の充足率で各経路の通りやすさが決まる → 実効ATPが出る。

**1gあたりの潜在ATP（モル数×100の相対値）**
```
糖質 17.8 ／ 脂質 41.4 ／ たんぱく質 15.7
```

**補酵素の需要**（`potential` は潜在ATPの合計）
```
B1: potential×0.012   B2: potential×0.011   B3: potential×0.012
B5: potential×0.010   B6: たんぱく質g×0.55
Fe: potential×0.008   Mg: potential×0.009
※ B3の供給には たんぱく質g×0.8 を加算（トリプトファンから作れるため）
```

**各経路の効率**＝関係する補酵素の充足率の最小値。下限（floor）付き。
```
解糖系 floor 0.35 ／ PDH 0.12 ／ β酸化 0.15 ／ アミノ酸 0.15 ／ TCA 0.15 ／ ETC 0.12
```

**酸素の上限**：ステージごとの `o2cap` は「NADH＋FADH₂の本数」の上限。
ATPに換算すると約2.3〜2.5倍になる点に注意（§5-1のバグの原因）。

ステージ定義は `STAGES` 配列、食材は `FOODS` 配列。

### 3-2. 肝臓の解毒工場

曝露（入ってくるもの）＋食事（支える材料）→ 第1相 → 反応性中間体 → 第2相 → 第3相。

**主要な定数**
```
抱合需要の係数    load × conj比率 × 0.70
第1相の速度       (0.40 + 0.60×補酵素充足率) × (1 + 0.5×CYP誘導)
中間体のGSH需要   R × 1.25
排出の需要        抱合済み量 × 0.25 + 2
負担 dmg = 残中間体×1.60 + 未抱合×1.10 + 第1相待ち×0.30 + 再吸収×0.35
肝臓の余力 = 100 − dmg×0.90
```

**特別ルール**：アセトアミノフェン（解熱鎮痛薬）とアルコールが同時にあるとき、
薬が反応性中間体に回る割合を1.7倍にしています。実際に知られている相互作用で、
このゲームで唯一「材料をどれだけ足しても埋まらない」組み合わせです。

**判定**：`余力 ÷ 目標` で S/A/B/C。ただし需要のある材料が空に近いときは
点数がよくてもS・Aを出さないよう上限をかけています（§5-2の対策）。

---

## 4. よくある改修の入口

| やりたいこと | 触る場所 |
|---|---|
| 食材を追加する | 各ゲームの `FOODS` 配列 |
| ステージを足す・難易度を変える | `STAGES` 配列（`target` と `fill` と `o2cap`） |
| 用語を追加する | 各ゲーム内の `GLOSSARY` オブジェクト（2ファイルに同じものが入っている） |
| 用語図鑑の記事を足す | `metabolism-glossary.html` の `PATHS` / `CHARS` / `DICT` 配列 |
| キャラクターを画像に差し替える | §4-1 参照 |
| SNSカード画像 | `ogp.png` を同名で差し替え（1200×630）|

### 4-1. キャラクター画像への差し替え方

**ゲーム内の鳥（結果発表の演出）**
`<svg class="bird" id="bird">…</svg>` を `<img class="bird" src="assets/characters/○○.png">` に置き換えます。
CSSアニメーション（`droop` / `bob` / `hop` / `flyup`）は `.bird` を対象にしているので、
imgに変えてもそのまま効きます。ランク別の出し分けは `.reveal[data-rank="C"]` などのセレクタです。
※ 羽ばたきだけは `#b-wingL` / `#b-wingR` を対象にしているので、imgにすると無効になります。

**用語図鑑の栄養素キャラクター13体**
`charSVG(c)` という関数がSVGを組み立てています。この戻り値を
`` `<img src="assets/characters/${c.id}.png" alt="">` `` に変えれば画像になります。
`CHARS` 配列の `id` がそのままファイル名になる作りです（b1, b2, b3, b5, b6, fe, mg, gsh, glu, sul, gly, atp, nadh）。

---

## 5. 過去にやらかしたこと（同じ轍を踏まないために）

### 5-1. クリア不可能なステージを渡した
酸素の上限を「ATPの量」で見積もるべきところ、「NADHの本数」で設定していました。
NADH 1本 ≒ ATP 2.5個なので、上限420本では電子伝達系が945ATPで頭打ちになり、
目標1700にはどう頑張っても到達できませんでした。
制作者が何度も挑戦して「難易度高くないですか？」と言うまで気づきませんでした。

**教訓：数値を変えたら、全ステージについて「良い献立」「そこそこの献立」「何も食べない」の
3パターンを計算し、それぞれA以上／目標未満／大きく未満になることを確認する。**

### 5-2. 表示と計算が食い違った
余力を計算する式の重みだけ更新し、結果画面の内訳グラフの重みを古いまま置き忘れました。
その結果、内訳を足しても余力の数字にならない状態になり、
さらに「グルタチオン0%」なのに「材料が十分だった。理想的な一日。」と表示されました。

**対策として、内訳の下に検算の1行を出すようにしています。**
「4つの合計 46 × 0.9 ＝ 42 が余力から引かれた（100 − 42 ＝ 58）」
数字が合わなくなったら画面を見た瞬間に分かります。この行を消さないでください。

### 5-3. SVGの色にCSS変数を使って見えなくなった
`circle.setAttribute('fill', 'var(--fadh)')` は動きません。
プレゼンテーション属性ではCSS変数が解決されないためです。**16進数で直接指定してください。**

---

## 6. 検算のやり方

JavaScriptのモデルを別言語（Pythonなど）で書き写し、
同じ入力で同じ答えが出ることを確かめる方法をとっています。手順は次のとおりです。

1. HTMLから `FOODS` / `STAGES` / `EXPOSURES` の配列を正規表現で抜き出す
2. `simulate()` と同じ計算をPythonで再現する
3. 代表的な献立の組み合わせを流し、スコアと判定を一覧で出す
4. 「良い献立でクリアできる」「手抜きだとクリアできない」が両方成り立つか見る

この方法だと、ブラウザを開かずに数十パターンを一度に確認できます。
実装を変えたら、写した側も必ず同時に直してください。

---

## 7. 公開の手順

1. リポジトリはPublicにする
2. `index.html` をリポジトリのルート直下に置く（フォルダの中に入れると404）
3. Settings → Pages → Build and deployment → Source を `Deploy from a branch`
4. Branch を `main`、フォルダを `/ (root)` にして Save
5. 1〜3分待つと、Pagesの画面に `Your site is live at …` と表示される
6. ファイルを更新すると自動で再デプロイされる（Actionsタブで進行状況が見られる）

**OGPについて**
4つのHTMLの `<head>` に `og:image` などが入っていて、URLは
`https://fiyoko510.github.io/karada-factory/` を前提に書かれています。
リポジトリ名やユーザー名を変える場合は、4ファイルすべてのURLを書き換えてください。

SNSはOGPをキャッシュするので、**投稿前にDiscordの自分用チャンネルにURLを貼って
表示を確認する**のが安全です。Xのカード確認ツールは廃止されています。

---

## 8. 制作者について知っておくとよいこと

- 返信は簡潔・スマホで読みやすい日本語を好みます
- 専門用語をそのまま出さず、必ず言い換えを添えてください
- エンジニア出身ではありませんが、GitHubの操作やFigmaは日常的に使います
- 鳥のモチーフが作品全体の通奏低音です（ウミネコが本人のアバター）
- 「難しくないですか？」と言われたら、まず自分の実装を疑ってください。過去2回とも実装側のバグでした

---

## 9. 免責

学習用の簡略化モデルです。ATPの収支や栄養素の必要量は一般的な推定値であり、
条件によって変わります。薬に関する記述は常用量を前提にした学習用の説明で、
実際の服薬判断の材料にはできません。この方針の文言（各ページのフッター）は
残したまま引き継いでください。
